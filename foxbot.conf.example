/* Config file for foxbot
 *
 * C/C++/Bash comments are allowed.
 *
 * I can't imagine there would be many more questions
 * on what each option does. (Yes, more to come soonish).
 */

/* bot {}: Bot options, major WIP */
bot {
    nick = "foxbot";
    ident = "fox";

    host = "misconfigured.network";
    port = "-4";
    channel = "#misconfigured";
    debug_channel = "#foxbot_logging";
    realname = ":3";
    // plugin = "echo_test.so";
};
